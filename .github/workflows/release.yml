name: Build and Release Firmware

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install PlatformIO
      run: |
        python -m pip install --upgrade pip
        pip install platformio
        
    - name: Build firmware
      run: |
        pio run
        
    - name: Create release package
      run: |
        python3 scripts/build_release.py
        
    - name: Create release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          .pio/build/modesp32v1/firmware.bin
          .pio/build/modesp32v1/firmware.elf
          .pio/build/modesp32v1/firmware_release.zip
        generate_release_notes: true
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        
    - name: Copy firmware to web directory
      run: |
        cp .pio/build/modesp32v1/combined_firmware.bin web/combined_firmware.bin
        cp .pio/build/modesp32v1/flash_addresses.txt web/flash_addresses.txt
        sed -i "s/Version: <span id=\"firmwareVersion\">.*<\/span>/Version: <span id=\"firmwareVersion\">${{ github.ref_name }}<\/span>/" web/index.html
        
    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./web 
